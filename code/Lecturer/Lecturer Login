*****import kivy
from kivy.app import App
from kivy.uix.label import Label
from kivy.uix.gridlayout import GridLayout
from kivy.uix.textinput import TextInput
from kivy.uix.button import Button

class MyGrid(GridLayout):
	def __init__(self, **kwargs):
		super(MyGrid, self).__init__(**kwargs)

		self.cols = 1

		self.inside = GridLayout()
		self.inside.cols = 3


		self.inside.add_widget(Label())
		self.inside.add_widget(Label(text="Lecturer Username: "))
		self.inside.add_widget(Label())
		self.inside.add_widget(Label())
		
		self.userName  = TextInput(multiline= False)
		self.inside.add_widget(self.userName)

		self.inside.add_widget(Label())
		self.inside.add_widget(Label())
		self.inside.add_widget(Label(text="Password: "))
		self.inside.add_widget(Label())
		self.inside.add_widget(Label())
		
		self.pword  = TextInput(multiline= False)
		self.inside.add_widget(self.pword) 
		self.inside.add_widget(Label())
		self.inside.add_widget(Label())

		self.add_widget(self.inside)

		self.submit = Button(text= "Submit", font_size= 40)
		self.submit.bind(on_press=self.pressed)
		self.add_widget(self.submit)

	def pressed(self, instance):
		print("pressed")
		usrname = self.userName.text
		password = self.pword.text
		print("UserName:", usrname ,"Password:", password )
		self.userName.text= ""
		self.pword.text= ""

class MyApp(App):
	def build(self):
		return MyGrid()

if __name__ == "__main__":
	MyApp().run()